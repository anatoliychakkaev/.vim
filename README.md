Install
=======

    # read+write access
    git clone git@github.com:anatoliychakkaev/.vim.git ~/.vim

    # readonly access
    git clone git://github.com/anatoliychakkaev/.vim.git ~/.vim

    ln -s ~/.vim/vimrc   ~/.vimrc
    ln -s ~/.vim/gvimrc  ~/.gvimrc
